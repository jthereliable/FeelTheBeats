require "coffee-script"

mongojs = require "mongjs"
settings = require "settings.json"

options = settings.database.mongodb

pool = mongojs "mongodb://#{options.host}:#{options.port}/#{options.database}?autoReconnect=true&poolSize=#{options.pool_size}"

exports = module.exports = pool